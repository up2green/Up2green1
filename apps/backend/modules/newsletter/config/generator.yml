generator:
  class: sfDoctrineGenerator
  param:
    model_class:           Newsletter
    theme:                 admin
    non_verbose_templates: true
    with_show:             false
    singular:              ~
    plural:                ~
    route_prefix:          newsletter
    with_doctrine_route:   true
    actions_base_class:    sfActions

    config:
      actions: ~
      fields:  ~
      list:
        display: [id, unique_name, reply_to, email_from, is_forced, sent_at]
      filter:  ~
      form:    ~
      edit:    ~
      new:     ~
